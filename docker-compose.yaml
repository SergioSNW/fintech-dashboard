# docker-compose.yml
version: '3.8'

services:
  fintech-dashboard:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=your_secret_key_here
      - DATABASE_URI=sqlite:///fintech_dashboard.db